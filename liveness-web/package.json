{
  "name": "privid-liveness-web",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone bundler for the Face Liveness web page embedded in the mobile WebView. NOT part of the backend npm workspace; its only output is api/public/liveness.bundle.js(+css), which is committed and served by the API.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "@aws-amplify/ui-react": "^6.5.5",
    "@aws-amplify/ui-react-liveness": "^3.1.3",
    "aws-amplify": "^6.6.0",
    "buffer": "^6.0.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.1"
  }
}
